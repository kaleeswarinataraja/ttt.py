#include <stdio.h>
int main()
{
    int l,m,n;
    scanf("%d %d %d",&a,&b,&c);
    if(l>m&&l>n)
    {printf("%d",l);}
    else if(m>l&&m>n)
    {printf("%d",m);}
    else
    {printf("%d",n);}
    return 0;
}
